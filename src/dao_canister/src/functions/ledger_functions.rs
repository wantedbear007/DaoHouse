// check balance
// transfer funds

use candid::{Nat, Principal};
pub async fn transfer_tokens(
    from: Principal,
    to: Principal,
    amount: Nat,
) -> Result<String, String> {

  

    Ok("()".to_string())
}
